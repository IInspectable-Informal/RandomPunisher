<local:PunishmentPage
    x:Class="RandomPunisher.Pages.Page6"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RandomPunisher.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ContentPresenter Content="{x:Bind TextControl, Mode=OneWay}" Grid.ColumnSpan="2"/>
        <ContentPresenter Content="{x:Bind NormalSize, Mode=OneWay}" Grid.Column="2"/>
        <ContentPresenter Content="{x:Bind FullAppView, Mode=OneWay}" Grid.ColumnSpan="3"/>
    </Grid>
</local:PunishmentPage>
