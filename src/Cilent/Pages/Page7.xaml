<local:PunishmentPage
    x:Class="RandomPunisher.Pages.Page7"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RandomPunisher.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ContentPresenter Content="{x:Bind TextControl, Mode=OneWay}"/>
        <ContentPresenter Content="{x:Bind NormalSize, Mode=OneWay}" Grid.Row="2"/>
        <ContentPresenter Content="{x:Bind FullAppView, Mode=OneWay}" Grid.RowSpan="2"/>
    </Grid>
</local:PunishmentPage>
